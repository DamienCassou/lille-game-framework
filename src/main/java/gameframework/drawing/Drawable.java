package gameframework.drawing;

import java.awt.Graphics;

/**
 * Classes implementing this interface can be drawn on the Canvas, using the method draw
 *
 */
public interface Drawable {
	public void draw(Graphics g);
}
